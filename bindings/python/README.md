# transport-core (Python bindings)

This directory contains the Python bindings for `transport-core`.

These bindings are intended to be consumed by higher-level ingestion
and consumer pipelines (e.g. lo-ingester).

This package is not yet published to PyPI and is intended to be used via:

- editable install, or
- Git dependency with subdirectory.
