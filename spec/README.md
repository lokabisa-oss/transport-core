# transport-core specification

This directory defines the canonical behavior of transport-core.

All implementations (native, WASM, reimplementations) MUST follow
the rules defined here.

Rust implementation is the reference, but behavior is defined
by this specification — not by code.
